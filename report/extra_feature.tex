\subsection{Extra features}
\subsubsection{Home page}
\label{subsubsec:home_page}
A good website should have a home page, so we also added one ourselves
(see figure \ref{fig:ux_dark_mode}). It provides a quick overview for the
whole website and also provide quick navigation.

\subsubsection{Event like buttons}
\label{subsubsec:event_like_buttons}
Social elements promotes user's interaction with the website. We added a like button
(similar to the favourite button) but with a like count, allowing user to see the most
liked events. (See figure \ref{fig:admin_crud_events})

\subsubsection{UX: dark mode}
\label{subsubsec:ux_dark_mode}
We provide three themes to promote user experience: system theme, light mode and dark mode.
On top of that, we added a transition animation (with the help of shadcn.io components)
to improve the transition (see figure \ref{fig:ux_dark_mode}).
\begin{figure}[h]
    \centering
    \begin{subfigure}{0.45\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figures/lightmode.png}
        \caption{Light mode}
        \label{subfig:light_mode}
    \end{subfigure}
    \begin{subfigure}{0.45\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figures/darkmode.png}
        \caption{Dark mode}
        \label{subfig:dark_mode}
    \end{subfigure}
    \begin{subfigure}{0.45\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figures/darkmode_transition.png}
        \caption{Dark mode transition animation}
        \label{subfig:dark_mode_transitiono}
    \end{subfigure}
    \caption{UX: dark mode}
    \label{fig:ux_dark_mode}
\end{figure}

\subsubsection{UX: loading screen}
\label{subsubsec:ux_loading_screen}
It is important to provide visual feedback for loading to improve UX and also avoid
confusion, especially for users with slow internet connection. For initial loading, and 
subsequent backend request for more than 300ms,
we show a loading screen animation (See figure \ref{fig:loading_screen}). Animation would not be shown for request shorter 
than 300ms to prevent rapid flashing. You can try this by running our database
with the \verb|-s| flag to simulate slow internet.

\subsubsection{UX: dynamic filtering}
In basic requirements, some pages are required to provide dynamic filtering.
We did one step further to provide it to all pages with tables (e.g. Map, UserManager).

\begin{figure}[h]
    \centering
    \includegraphics[width=0.8\textwidth]{figures/Loading_screen.png}
    \caption{Loading screen}
    \label{fig:loading_screen}
\end{figure}

\subsubsection{UX: feedback messages}
\label{subsubsec:ux_feedback_messages}
Feedback messages are important for user to know what is happening behind the scene.
While it is possible to use \verb|alert|, it doesn't match the UI and it annoys user
by forcing them to click a button just for a message. To avoid this, we simply show
a message with different colors for different types of messages (see figure \ref{fig:feedback_message}).

\begin{figure}[h]
    \centering
    \begin{subfigure}{0.45\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figures/feedback_1.png}
        \caption{User waiting database response}
        \label{subfig:feedback_1}
    \end{subfigure}
    \begin{subfigure}{0.45\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figures/feedback_2.png}
        \caption{User receiving success response}
        \label{subfig:feedback_2}
    \end{subfigure}
    \begin{subfigure}{0.45\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figures/feedback_3.png}
        \caption{Alert message to prevent rapid request}
        \label{subfig:feedback_3}
    \end{subfigure}
    \caption{Feedback messages}
    \label{fig:feedback_message}
\end{figure}

\subsubsection{UX: refresh button}
\label{subsubsec:ux_refresh_button}
All our tables come with a refresh button so that user could re-sync their data with
database.

\subsubsection{Security: Prevent rapid request}
\label{subsubsec:security_prevent_rapid_request}
For impatient users with slow internet access or users with malicious intent, they
may want to spam our server with HTTP requests. To prevent this, we added a loading
tracker in frontend to block further request if we currently have a queueing request.
(See figure \ref{subfig:feedback_3} as we show an alert message)

\subsubsection{Security: reCAPTCHA}
\label{subsubsec:security_reCAPTCHA}
To prevent bots abusing our service or ddos attack, we require user
to pass Google reCAPTCHA before login / sign up.
As shown in figure \ref{fig:non_user_req}, user are not able to sign in without completing the reCAPTCHA.

Note: User cannot bypass this even if they hack the frontend, as the reCAPTCHA token will be
verified in the backend.
\subsubsection{Security: password hashing}
\label{subsubsec:password_hashing}
To prevent password leak and to protect user privacy, we hash the password when
user sign up. As shown in figure \ref{fig:pw_hashing}, the password \verb|csci2720| became a long
encrypted string, which can only be verified with our secret key.
\begin{figure}[h]
    \centering
    \includegraphics[width=0.8\textwidth]{figures/pw_hashing.png}
    \caption{Password hashing}
    \label{fig:pw_hashing}
\end{figure}

\subsubsection{Security: token and admin authorization}
\label{subsubsec:token_and_admin_authorization}
While we use browser local storage to store user login status, we know it is not safe
as frontend data can be easily modified. To prevent malicious attempt, we store a 
token in user's local storage when they login. All attempts to assess the server must
first pass through our \verb|require-auth| middleware with the token. (See figure \ref{subfig:security_token})
You can try this by clearing browser data after login, then navigate different pages.
Don't refresh the page as that will send you to back to authentication.

On top of that, admin-only routes has to pass through an addition
\verb|require-admin| middleware with their userID. It will be compared with the database
to check if they are really admin.  (See figure \ref{subfig:security_admin})
You can try this by changing your role from "admin" to "user" on the database after
logging in as an admin.

\begin{figure}[h]
    \centering
    \begin{subfigure}{0.8\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figures/security_token.png}
        \caption{User access to server rejected without valid token}
        \label{subfig:security_token}
    \end{subfigure}
    \begin{subfigure}{0.8\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figures/security_admin.png}
        \caption{User access to admin-only page rejected due to invalid admin user ID}
        \label{subfig:security_admin}
    \end{subfigure}
    \caption{Security: token and admin authorization}
    \label{fig:security_token_and_admin}
\end{figure}